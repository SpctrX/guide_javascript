console.log('\n========== [Beispiel: 4] =========='); 
let zahl;                   // definieren von 'zahl'
zahl = Math.random() * 10;  // 'zahl' definieren von 0 - 10 (default: 0 - -1)
console.log(zahl);          // Ausgabe von 'zahl' in die Console